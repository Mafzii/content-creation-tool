[project]
name = "content-tool-mcp"
version = "0.1.0"
description = "MCP server wrapping the content creation tool REST API"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
